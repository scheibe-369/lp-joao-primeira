---
import Container from '../../components/ui/Container.astro';
import Icon from '../../components/ui/Icon.astro';

const items = [
  {
    icon: 'shirt',
    title: 'Estampe qualquer tecido',
    text: 'Algodão, poliéster, nylon, jeans e malhas — cores vivas e toque leve.',
  },
  {
    icon: 'coins',
    title: 'Baixo custo, alta margem',
    text: 'Custo baixo por estampa e ótima margem por peça vendida.',
  },
  {
    icon: 'rocket',
    title: 'Comece em casa e escale',
    text: 'Da bancada A3 até a produção industrial, no seu ritmo.',
  },
] as const;
---

<section class="border-y border-line bg-surface py-12 sm:py-14">
  <Container>
    <h2 class="sr-only">Por que escolher DTF</h2>
    <ul class="grid gap-6 sm:grid-cols-3">
      {
        items.map((it, i) => (
          <li class="on-dark flex items-start gap-4 rounded-2xl bg-navy p-6 shadow-elev-2" data-reveal data-reveal-delay={i * 80}>
            <span class="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl bg-accent text-accent-ink">
              <Icon name={it.icon} size={22} />
            </span>
            <div>
              <h3 class="font-display text-xl">{it.title}</h3>
              <p class="mt-1 text-sm leading-relaxed text-muted">{it.text}</p>
            </div>
          </li>
        ))
      }
    </ul>
  </Container>
</section>
